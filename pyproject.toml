[project]
name = "caching-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "argparse>=1.4.0",
    "fastapi[standard]>=0.116.1",
    "httpx>=0.28.1",
    "redis[hiredis]>=6.4.0",
    "uvicorn>=0.35.0",
]
